<% if ENV['MONGOLAB_URI'] %>
  <% uri = URI.parse(ENV['MONGOLAB_URI']) %>
development:
  sessions:
    default:
      database: <%= uri.path.sub('/','') %>
      username: <%= uri.user %>
      password: <%= uri.password %>
      hosts:
        - <%= uri.host %>:<%= uri.port %>
<% end %>

test:
  sessions:
    default:
      uri: <%= ENV['MONGOLAB_URI'] %>
production:
  sessions:
    default:
      uri: <%= ENV['MONGOLAB_URI'] %>