development:
  sessions:
    default:
      uri: <%= (ENV['MONGOLAB_URI'] || "localhost:27017/mongoid") %>
      options:
        skip_version_check: true
        safe: true
test:
  sessions:
    default:
      uri: <%= ENV['MONGOLAB_URI'] %>
      options:
        skip_version_check: true
        safe: true
production:
  sessions:
    default:
      uri: <%= (ENV['MONGOLAB_URI'] || "localhost:27017/mongoid") %>
      options:
        skip_version_check: true
        safe: true